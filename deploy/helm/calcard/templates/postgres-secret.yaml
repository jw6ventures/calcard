{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "calcard.fullname" . }}-postgres-secret
  labels:
    {{- include "calcard.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.app.db.password | quote }}
{{- end }}
