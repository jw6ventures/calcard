apiVersion: v1
kind: Service
metadata:
  name: {{ include "calcard.fullname" . }}
  labels:
    {{- include "calcard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calcard.selectorLabels" . | nindent 4 }}
